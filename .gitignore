# Binários
mediamtx
*.tar.gz

# Configuração com senhas reais (nunca commitar)
# Os templates em config/ contêm apenas placeholders
mediamtx.yml.local
frigate.yml.local

# Gravações e snapshots do Frigate (dados pesados)
storage/

# OS
.DS_Store
Thumbs.db
